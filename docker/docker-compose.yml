version: '3.7'

services:
  bot:
    restart: always
    build:
      context: ../
      dockerfile: ./docker/bot/Dockerfile
    environment:
      - ENV=${ENV}
      - VER=${VER}
      - TZ=${TZ}
      - SENTRY_DSN=${SENTRY_DSN}
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ../../data:/home/app/data

  nginx:
    restart: always
    image: nginx:latest
    volumes:
      - ../nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - bot
