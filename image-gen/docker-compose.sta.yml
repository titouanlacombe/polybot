version: '3.7'

services:
  bot:
    volumes:
      - ../bot/src:/home/app/src
      - ../../data:/home/app/data

  image-gen:
    volumes:
      - ../image-gen/src:/home/app/src
      - ../../data:/home/app/data

  nginx:
    ports:
      - 5001:80
